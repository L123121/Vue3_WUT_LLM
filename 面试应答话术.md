# 面试应答话术 — 武理小精灵 V2.0 改进复盘

> 依据 2026-08 面试官反馈（5 条建议）+ 本次 V2.0 落地内容编写。
> 每条话术结构：**面试官问什么 → 怎么答（核心逻辑）→ 关键话术 → 踩坑/数据支撑**。
> 配套文档：`V2.0改进方案.md`（技术方案）、`CLAUDE.md`（架构与调优记录）。

---

## 建议一：加意图识别层自动路由，别让用户手动开关 RAG（⭐⭐⭐⭐⭐）

### 面试官可能这么问
- "你们 RAG 是用户手动开关的吗？用户怎么知道该不该开？"
- "如果用户不想查知识库，只是随便聊聊呢？"

### 怎么答（核心逻辑）
先承认问题 → 说明 V2.0 已改为自动路由 → 讲清三层设计 → 用数据说明为什么默认走 RAG。

### 关键话术
> "您说得对，V1.0 确实把'该不该查校内知识库'这个决策交给了用户，产品上不友好。V2.0 我在后端加了一层**意图识别自动路由**，用户只负责提问，系统自动决定走哪条链路。
>
> 路由分三层：
> 1. **零成本关键词路由**（fastRoute）：只保留高置信、几乎不会误判的规则——问候/感谢/告别直接走纯 LLM 对话（不触发检索，快、省）；明确的多步任务（'帮我制定复习计划'）走工具调度。这条路径**零 LLM 调用**，不增加首包延迟。
> 2. **LLM 分类兜底**（可开关）：规则无法确定时让 LLM 用 JSON 输出分类，15 秒超时兜底，失败自动降级。
> 3. **默认兜底 = 知识库检索**：校园问答 80%+ 是事实性知识查询，知识库是最大价值，所以默认走 RAG 检索管道；而 RAG 管道内部本来就有'检索不到可靠来源 → 降级纯 LLM'的兜底，纯闲聊不会被误伤。
>
> 一句话：**用系统的路由取代用户的选择，让'查不查知识库'对用户无感知。**"

### 踩坑/数据支撑
- 前端已移除手动 RAG 开关（`ChatBox.vue`），改为消息气泡上展示"自动路由：知识库检索"徽标，透明但不打扰。
- SSE 新增 intent 事件下发给前端；`INTENT_ROUTING_ENABLED=false` 可整体回退原链路。
- 后端单测 25 个（正向匹配 + 边缘意图不硬路由 + LLM 兜底/超时降级），全量 188 用例通过。

---

## 建议二：Agent 化，工具调度 + 多轮上下文（⭐⭐⭐⭐）

### 面试官可能这么问
- "你们有考虑做成 Agent 吗？"
- "多轮对话你怎么处理？哪些算上一轮的上文？"
- "为什么你之前把 Agent 移除了，现在又提 Agent？"

### 怎么答（核心逻辑）
**先讲清楚"回退"这段历史**（这是加分项，证明你有真实工程判断）→ 再讲 V2.0 的折中方案（多轮工具调度 + 原生 function calling）→ 最后讲多轮上下文方案（L3 记忆）。

### 关键话术
> "我确实做过完整 Agent，也踩过坑。2026-07 我实现过一版 ReAct 多步循环 Agent（12 个工具：查成绩、查课表、天气、计算……），但**上到真实场景后主动回退了**，原因有三个：
> 1. **延迟**：校园场景 80%+ 是单跳查询，ReAct 至少 2-3 轮 LLM 调用，首 token 从秒级拖到十几秒，用户感知明显变差；
> 2. **复杂度**：多步循环需要工作记忆、循环检测、超时熔断，维护成本高；
> 3. **产品**：前端展示'思考过程'对校园用户是减分项，普通用户只想要答案。
>
> 所以 V2.0 我做的是**有界多轮工具调度 + 原生 function calling**：第一次 LLM 调用直接走 OpenAI 兼容的原生工具协议（payload 带 `tools` schema，流式解析 `delta.tool_calls` 增量拼接），决定调 `search_knowledge_base` 还是 `calculate`；确定性执行工具（带超时闸门）；工具结果以 `tool` 角色消息回注，**允许最多 2 轮再决策**，然后强制收尾生成。**关键在'有界'**：轮次上限（`AGENT_MAX_TOOL_ROUNDS=2`）+ **无进展检测**（连续 2 轮相同工具调用签名 → 强制收尾防死循环），既保留 Agent 的'工具抽象 + LLM 决策 + 多步规划'，又避开了被回退的无限循环。
>
> 多轮上下文我做的是 **L3 会话级结构化记忆**（`buildMemorySummary`）：从 history 提取'最近提问 + 上一轮回答结论'注入 system prompt，解决'上一轮说的高数，这轮问那道题'的指代断裂——**不引入 Redis 工作记忆**（回退教训），随请求重算，零持久化成本。另外每条 Agent 回答都会下发结构化 trace（轮次/工具/耗时/收尾原因），可观测性也是 Agent 落地的关键一环。"

### 踩坑/数据支撑
- 存档在 `D:\武理小精灵_agent_存档`，README 完整记录了回退决策（2026-07-21）。
- V2.0 工具层：`tool-registry.service.js`（注册/超时/开关）+ `agent-tools.js`（2 个工具，mathjs 安全求值防注入）+ `agent.service.js`（**多轮调度**，`AGENT_TOOL_ENABLED=true` 灰度开关，默认关不影响评测基线）。
- L2 多轮 + L3 记忆 + agentTracer（2026-08-14 落地）；agent-tools 22 个单测通过（含无进展检测、轮次上限、记忆提取、trace 事件），后端全量 200 用例通过。

---

## 建议三：re-rank 更精准，而不是扩大上下文窗口（⭐⭐⭐⭐）

### 面试官可能这么问
- "召回内容多了怎么办？动态 K 值行不行？"
- "你的上下文窗口为什么是 6000 字？"

### 怎么答（核心逻辑）
先认同"少而精"原则 → 讲现有精排链路的三个手段 → 讲为什么不用动态 K 值扩大窗口。

### 关键话术
> "您说得对，**上下文窗口不应该作为'召回不够准'的兜底**——窗口越长，模型越容易受噪声干扰、越容易幻觉，成本也越高。V1.0 就按这个原则设计了精排链路：
> 1. **BGE-reranker-base cross-encoder 逐对精排**：把 50 个候选句子按父段归并成 ~15 个，逐对打分，比向量余弦更精准；
> 2. **自适应截断**：断崖检测（相邻分差 > 0.05）+ 低分过滤（< 0.3）+ 硬上限（rerankTopK=10），三道闸门只留高置信片段；
> 3. **MMR 语义去重**：同文档内高度相似的父段（bigram Jaccard ≥ 0.85）剔除，防止相似段落挤占上下文。
>
> 当时我提过'动态 K 值'，您的反馈点醒了我——**问题不在 K 值大小，而在精排质量**。后续我会做两件事：一是 reranker 分数归一化校准（现有 `_normalizeScore` 基础上评估更激进的截断档位 0.35/0.4）；二是把上下文从 6000 字降档到 4000 字做 A/B，验证'更少更准的上下文'指标不降反升。"

### 踩坑/数据支撑
- MMR 去重有过真实回归（2026-08-09）：跨文档相似度 1.0 导致第二个相关文档整体被剔，Recall 97.4%→80.7%；修复为"仅同 docId 内去重"后回 97.4%，MRR 0.977、nDCG@5 0.970、HitRate 100%。→ 面试讲这个最能体现工程复盘能力。
- 官方评测体系：Recall/MRR/nDCG@5/HitRate，回归阈值 2%。

---

## 建议四：知识库入库人工审核，从源头保证质量（⭐⭐⭐）

### 面试官可能这么问
- "两份文件答案矛盾怎么办？"
- "检索召回不准，你打算怎么优化？"

### 怎么答（核心逻辑）
先认同"数据质量 > 算法优化" → 给出三件套方案（审核流/冲突检测/健康度）→ 说明当前已具备的技术基础。

### 关键话术
> "您说得对，**脏数据进去，再好的检索和重排都救不回来**——召回问题应该回到源头解决。我的规划三件套：
> 1. **入库审核流**：`indexing.service.js` 入库前先落 pending 状态，管理员审核通过才进向量库（Qdrant），未审核文档不参与检索；
> 2. **冲突检测**：同类别文档入库时与既有文档算 bigram Jaccard 相似度（复用现有 `_jaccardBigrams`，零模型成本），> 0.85 提示'疑似重复/冲突'，人工决定保留哪个、是否加权威性优先级；
> 3. **健康度监控**：统计每文档检索命中率、来源引用率、低分片段占比，定期输出报告，主动发现过期/冗余内容。
>
> 技术上这些都是复用现有能力的增量：文档解析、向量化、相似度计算、管理接口都已具备，缺的是把'入库'从'直接进库'改成'审核后进库'的流程闸门。"

### 踩坑/数据支撑
- 现有 `_inferDocCategory` 元数据过滤 + 自动类别推断已有雏形（2026-08-08），跨文档误伤时自动回退全库（trace 记录 autoCategory/fallback）。
- 面试金句：**"冲突解决靠审核优先级，而不是靠检索算法碰运气。"**

---

## 建议五：技术选型要有工业级扩展性视角（⭐⭐⭐）

### 面试官可能这么问
- "为什么用 SQLite？用户量大了怎么办？"
- "为什么用阶跃星辰的模型？"

### 怎么答（核心逻辑）
不回避"从简单起步"，但要**讲清演进路径**——个人项目先验证，架构上留好替换口。

### 关键话术
> "选型原则是**先验证、后扩展，但架构上从第一天就留好替换口**：
> - **存储**：SQLite 单机够用、零运维，适合验证阶段；演进路径是 PostgreSQL + pgvector（或继续用 Qdrant）。实际上向量库我们已经从 Milvus 迁到了 **Qdrant 独立服务**（`docker-compose.qdrant.yml` 已就绪），天然支持多副本分片，本地文件版（`VECTOR_STORE_BACKEND=file`）保留可切换——这本身就是'单机起步、服务化演进'的实例。
> - **LLM**：阶跃星辰免费起步是现实考量，但接入层是 **OpenAI 兼容协议**（`_normalizeProvider` 支持主/备双 provider 自动切换），换模型/多模型灰度只改配置不动代码；评测链路用了**独立 API Key + 独立模型**（step-3.5-flash），保证评测不抢生产配额——这个隔离在工业界也是标准做法。
> - **成本控制**：LLM 请求队列（并发 3）+ 上下文压缩 + 意图路由省调用，都是'上量前先管住成本'的意识。
>
> 如果面试官追问'用户量到多少要换'：我的回答是**先看瓶颈在哪**——Qdrant 扛不住是分片扩容，LLM 贵是缓存+精排降调用量，SQLite 扛不住才迁 PG；不是一上来就上最重的架构。"

### 踩坑/数据支撑
- 历史教训：Redis Cloud 跨洋 hgetall 2-3s 拖垮单次 RAG 到 55s，改用本地 MemoryStore 后 listDocuments 32s→5ms（2025-07-13）。→ 说明"上重组件要看实际瓶颈"。

---

## 面试官隐含提醒：产品思维 > 技术实现

> "面试官反复追问'用户怎么知道该不该开 RAG'，本质是在考察**你是否站在用户视角思考**。技术再牛，产品设计反人类就是硬伤。"
> "好的工程不是炫技，是**找到最稳健、最可维护的解决方案**——动态 K 值的否定、知识库审核的建议，都指向同一件事。"

**怎么内化到话术**：每次回答技术方案，都主动补一句"这对用户意味着什么"。例如：
- 自动路由："用户不用知道 RAG 是什么，问就完了。"
- 单轮工具调度："用户只关心答案来得快不快、准不准，不关心背后调了几次工具。"
- 知识库审核："用户问'食堂几点关门'，两个文档给两个答案，比检索慢 200ms 更伤信任。"

---

## 快速自测清单（面试前过一遍）

| 问题 | 一句话回答 |
|:---|:---|
| 为什么移除 Agent？ | 校园 80%+ 单跳查询，ReAct 延迟/复杂度/思考展示都是减分项（2026-07 存档） |
| V2.0 自动路由怎么做的？ | fastRoute 零成本关键词 + LLM 分类兜底 + 默认走 RAG（管道内部自带降级） |
| 多轮上下文怎么处理？ | 滚动压缩 + query 改写，不引入 Redis 工作记忆（回退教训） |
| 为什么不用动态 K 值？ | 上下文不是召回不准的兜底；精排（reranker + 自适应截断 + MMR）才是 |
| 知识库质量怎么保证？ | 入库审核 + 冲突检测（bigram Jaccard）+ 健康度监控 |
| SQLite 用户量大怎么办？ | 演进路径明确（Qdrant 已服务化 / PG+pgvector），架构留替换口 |
| 为什么用免费模型？ | 现实起步，但 OpenAI 兼容协议 + 双 provider + 评测 Key 隔离，工业级替换口 |
| 前端移除开关后怎么知道走了哪条链路？ | SSE intent 事件 + 气泡"自动路由：知识库检索"徽标 |
