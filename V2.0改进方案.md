# 武理小精灵 V2.0 改进方案

> 依据 2026-08 面试官反馈（5 条核心建议）+ 存档 Agent 代码（`D:\武理小精灵_agent_存档`）评估产出。
> 目标：把"手动开关 RAG"升级为"意图识别自动路由"，并把各环节沉淀为可复用的工具层，同时补齐知识库质量与选型论证。

## 一、面试官反馈 → 改进项映射

| # | 面试官建议 | 改进项 | 优先级 | 落地阶段 |
|:--:|:---|:---|:---:|:---:|
| 1 | 加意图识别层自动路由，别让用户手动开关 RAG | 意图识别 + 自动路由（IntentRouter） | ⭐⭐⭐⭐⭐ | P0 |
| 2 | 做成完整 Agent，工具调度 + 多轮上下文 | 轻量 Agent 工具层（Tool Registry + 单轮工具调度） | ⭐⭐⭐⭐ | P1 |
| 3 | re-rank 更精准，别扩大上下文窗口 | 精排阈值校准 + 上下文"少而精" | ⭐⭐⭐⭐ | P1 |
| 4 | 知识库入库人工审核把关 | 入库审核 + 冲突检测 + 健康度监控 | ⭐⭐⭐ | P2 |
| 5 | 选型要有工业级扩展性视角 | 选型论证文档 + 可替换抽象 | ⭐⭐⭐ | P2 |

## 二、现状问题定位（调研结论）

### 当前链路（2026-08-14）
```
前端 RAG 开关（ChatBox.vue 手动 ON/OFF）
  ↓ mode 传给后端
后端 streamHandler：问候语正则（5 个词）跳过 RAG，其余一律走 RAG 检索管道
  ↓ rag.service.chatStream：检索 → 重排 → 截断 → 上下文 → LLM
```

**问题**：
1. **路由决策前置给了用户**——"该不该查校内知识库"由用户点开关决定，与面试官"用户无感知"的原则相反。
2. 后端已有 `classifyQuestion`（权威/事实/知识/通用四分类）和 `_inferDocCategory`（文档类别推断），但都只服务于提示词/过滤，**没有用于路由**。
3. 无多轮上下文显式处理（靠 `history` 原样传入），无工具抽象。

### 存档 Agent 代码复用评估（关键约束）
存档是 2026-07-21 因"校园场景 80%+ 单跳查询、ReAct 多步循环带来延迟与复杂度、前端展示思考过程对校园用户是减分项"而**回退**的版本。直接结论：

| 存档模块 | 复用价值 | 结论 |
|---|---|---|
| `intent-router.service.js`（fastRoute 关键词 + LLM 分类兜底） | **高**——零 LLM 调用的高置信关键词路由设计完全可移植 | ✅ 直接借鉴，裁剪为校园问答路由（rag/chat/agent） |
| `tool-registry.service.js`（通用工具注册表） | **高**——工具注册/执行/超时/开关的通用抽象 | ✅ 精简移植（去掉教务绑定部分） |
| `agent-tools.js` 的 `search_knowledge_base` 工具 | **高**——把 RAG 检索封装为工具的标准写法 | ✅ 移植 |
| `react-agent.service.js`（ReAct 多步循环） | **低**——正是被回退的部分：延迟高、展示思考过程减分 | ⚠️ 不整体移植；只取其"单轮工具调用流式解析"能力，做**单轮工具调度**（详见 P1） |
| `working-memory.service.js` / `working-memory-store.js` | **中**——跨轮上下文管理有价值，但依赖 Redis | ⚠️ 只取"跨轮摘要"思路，复用现有 `_compactHistory` |
| 教务工具（query_grades 等 9 个） | **低**——当前项目无教务系统接入 | ❌ 不移植 |

## 三、P0：意图识别 + 自动路由（本次落地）

### 设计
```
用户提问
  ↓
IntentRouter.route(message)
  ├─ fastRoute(): 高置信关键词路由（零 LLM 调用，~0ms）
  │    rag:    明确校内知识问题（食堂/图书馆/课程/政策/算法题…）
  │    chat:   问候/感谢/寒暄（零误判词）
  │    agent:  明确的多步/复合问题（选课可行性/对比分析）
  │    null → 降级
  ├─ 规则无法确定且配置了 apiKey → LLM 分类（15s 超时，JSON 输出）
  └─ 兜底：默认走 rag（校园问答主场景，知识库是最大价值）
  ↓
路由执行
  ├─ rag   → 现有 rag.service.chatStream（检索→重排→LLM）
  ├─ chat  → 纯 LLM 流式（不触发检索，快、省）
  └─ agent → P1 工具调度（search_knowledge_base + 计算等）
```

### 关键设计原则
1. **fastRoute 只保留"零误判"规则**（存档原话：低置信正则硬路由会误伤 + 架空 LLM 决策）。
2. **默认兜底 = rag**，不是 chat——校园问答主场景是知识库；问候类被 fastRoute 截走，其余走 RAG 才能保证答案质量。
3. **路由结果随 SSE trace 下发**（`intent`/`route` 字段），前端可展示"自动路由：知识库检索"增强透明感（不再需要开关）。
4. 前端**移除手动 RAG 开关**，改为自动路由 + 路由状态展示。

### 涉及文件
- 新增 `backend/src/services/intent-router.service.js`（裁剪存档版：路由表改为 rag/chat/agent 三类）
- 修改 `backend/src/controllers/chat.controller.js`（streamHandler/chatHandler 接入路由）
- 修改 `backend/src/config/index.js`（`rag.intentRouting` 开关 + `INTENT_ROUTING_ENABLED` 环境变量）
- 修改 `src/components/chat/ChatBox.vue`（移除开关按钮）
- 修改 `src/views/AIChat.vue` + `src/api/chat.js` + 相关 store（mode 参数改为后端自动路由，展示 intent 徽标）
- 新增 `backend/__tests__/intent-router.test.js`（移植存档测试并改造）

## 四、P1：轻量 Agent 工具层（本次落地）

### 设计（吸取回退教训）
不做 ReAct 多步循环，做**单轮工具调度（tool routing）**：

```
agent 意图
  ↓
LLM 一次调用（带工具 schema）→ 决定调用 search_knowledge_base / calculate / 直接回答
  ↓
执行工具（本地，秒级）→ 结果拼入上下文 → LLM 生成最终答案
  ↓
全程 1 轮工具调用，无多轮循环，延迟 ≈ RAG 链路 + 1 次 LLM 调用
```

- 工具层：`tool-registry.service.js`（注册/执行/超时/开关，精简移植）+ `agent-tools.js`（`search_knowledge_base` 复用 rag.service，`calculate` 本地 mathjs）。
- **多轮上下文**：不引入 Redis 工作记忆（避免回退教训），复用现有 `_compactHistory` 滚动压缩 + query 改写（现有 `shouldRewriteQuery`/`rewriteQuery` 已具备）。
- 开关：`AGENT_TOOL_ENABLED`（默认 false，先灰度，避免影响现有评测基线），可通过 `RAG_FUSION` 同级的 env 切换。

### 为什么不再做完整 ReAct
存档 README 明确记录：80%+ 单跳查询不需要多步规划；多步循环的延迟（2 次以上 LLM 调用）与前端思考展示对校园用户是减分项。**单轮工具调度**保留了 Agent 的"工具抽象 + LLM 决策"，去掉了被回退的部分（多步循环 + 思考展示），是两者最优折中——这正是面试回答"Agent 化怎么做"时可以讲清楚的设计取舍。

## 五、P1：re-rank 精度优化（不扩窗）

面试官原话："让 re-rank 更精准，而不是扩大上下文窗口"。当前已有：
- 自适应截断（断崖 > 0.05 + 低分 < 0.3 + 硬上限 rerankTopK）
- MMR 去重（同 doc 内相似段落剔除，跨文档不误伤）

**进一步动作**（本次不实现，P1 末评估）：
1. 对 reranker 分数做**归一化校准**：`_normalizeScore` 已有，评估是否对上下文截断更激进（如 0.35/0.4 阈值档位）。
2. 上下文 `maxContextLength` 6000 → 可选降档 4000 的 A/B（"少而精"验证）。
3. 核心指标：官方评测 Recall/MRR/nDCG 不回退 + 上下文 Token 下降。

## 六、P2：知识库质量（人工审核 + 冲突检测）

面试官原话："解决知识库的问题而不是解决召回的问题，入库做审核。"

设计（建议后续单独排期）：
1. **入库审核流**：`indexing.service.js` 入库前先落 `pending` 状态 → 管理员接口审核通过才进向量库（Qdrant 已有 `reindex` 路径可接）。
2. **冲突检测**：同类别文档入库时计算与既有文档的 bigram Jaccard 相似度（复用 `_jaccardBigrams`），> 0.85 提示"疑似重复/冲突"。
3. **健康度监控**：定时任务统计每文档检索命中率、来源引用率、低分片段占比，输出报告。

## 七、P2：选型论证（工业级视角）

面试追问："用户量大怎么选型"。为简历/面试准备，落一份 `docs/技术选型.md`，覆盖：
- **存储**：SQLite（当前，单机够用）→ 用户量大时的演进路径：PostgreSQL（JSONB 向量 + pgvector）或保留 Qdrant（已支持独立服务部署，`docker-compose.qdrant.yml` 已有）。
- **向量库**：已从 Milvus → Qdrant（独立服务），天然支持横向扩展（多副本 + 分片），比本地文件版（`VECTOR_STORE_BACKEND=file`）更适合上量。
- **LLM**：StepFun 免费起步 → 工业级考虑：多 Provider 抽象（OpenAI 兼容协议已满足，`_normalizeProvider` 已有）→ 按量计费/多模型灰度/自建推理（vLLM）的演进。
- **评测**：已独立 Key 隔离，工业级再加：回归流水线（CI 触发）+ 黄金数据集版本管理。

## 八、实施路线图

| 阶段 | 内容 | 验收标准 |
|:--:|:---|:---|
| P0（本次） | 意图路由层 + 前端去开关 | 后端单测通过；前端构建通过；官方评测 Recall/MRR 不回退 |
| P1（本次） | 轻量 Agent 工具层（默认关） | 工具注册/执行单测通过；`AGENT_TOOL_ENABLED=true` 手动冒烟 |
| P1（后续） | rerank 阈值校准 + 上下文降档 A/B | 评测指标 + Token 消耗对比表 |
| P2（后续） | 入库审核 + 冲突检测 + 健康度 | 管理接口冒烟 + 冲突样例检出 |
| P2（后续） | 技术选型文档 | 文档产出，可支撑面试追问 |

## 九、与存档代码的复用对照表（完整）

| 存档文件 | 本次动作 | 说明 |
|---|---|---|
| `intent-router.service.js` | 移植裁剪 | fastRoute/LLM 兜底结构保留，路由表改 rag/chat/agent |
| `tool-registry.service.js` | 精简移植 | 保留注册/超时/开关/统计，去掉教务与 Redis 依赖 |
| `agent-tools.js` | 部分移植 | 仅 `search_knowledge_base` + `calculate` 两个工具 |
| `agent-handlers.js` | 不移植 | 其 simple 路径绑教务工具；knowledge 路径逻辑并入 rag 路由 |
| `react-agent.service.js` | 不移植 | 回退原因仍成立；取其单轮工具流式解析思路 |
| `working-memory*.js` | 不移植 | Redis 依赖 + 复杂度，用现有压缩/改写替代 |
| `agent-tracer.js` | 不移植 | 现有 tracer 已覆盖 trace 事件 |
