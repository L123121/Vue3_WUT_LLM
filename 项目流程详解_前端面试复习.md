# 武理小精灵：项目流程详解（按当前代码库）

> 这份文档基于当前仓库源码整理，面向前端面试复习。重点不是背技术名词，而是能讲清楚：页面怎么启动、消息怎么流式回来、状态怎么拆、接口在哪里、哪些是已实现能力，哪些只是预留或后续优化点。

---

## 0. 面试速记版

### 一句话介绍

武理小精灵是一个面向武汉理工大学校园场景的 **Vue 3 + Pinia + Express** AI 助手，前端实现聊天（含语音输入）、知识库、评测、设置和个性化配置，后端提供 RAG 检索链路、教务工具、会话持久化、安全鉴权和评测体系。

### 三句话讲项目价值

1. **用户体验**：聊天页支持 Fetch 流式输出、语音输入、附件上传、Markdown 渲染、代码高亮和多会话管理。
2. **智能能力**：后端 RAG 检索链路（BGE-small-zh + Milvus Hybrid Search + BGE-reranker-base），支持父子段落检索和自适应截断。
3. **工程能力**：项目有统一 API 封装、Pinia 职责拆分、本地缓存 + 后端会话、httpOnly cookie 鉴权、评测体系（LLM-as-judge 独立 Key + 人工抽检 + 线上反馈）、Vitest 测试和 Docker/Nginx 部署配置。

### 面试优先讲的亮点

| 亮点 | 按实际项目怎么讲 |
| --- | --- |
| 流式聊天 | 前端用 `fetch` + `ReadableStream.getReader()` 解析 SSE `data:` 行，边收边更新 AI 消息。 |
| 状态拆分 | `chat.store` 聚合入口，`conversation.store` 管会话数据，`message.store` / `useStreaming` 管流式过程。 |
| 语音输入 | 基于 Web Speech API 的语音识别，实时转文字填入输入框。 |
| RAG | 本地 BGE-small-zh ONNX + Milvus Hybrid Search（稠密×0.6 + 稀疏×0.4）+ BGE-reranker-base 重排 + 父子段落。 |
| 评测 | LLM-as-judge 独立 API Key + step-3.5-flash，4 指标合并 1 次请求，失败降级关键词匹配。 |
| 性能 | 长消息 Markdown 超阈值走 Worker，流式 chunk 用 `requestAnimationFrame` 合并更新。 |
| 安全 | 登录用后端 httpOnly cookie，前端只存用户展示信息；Markdown 用 DOMPurify；上传文件做类型限制和 `nosniff`。 |

### 当前代码里的真实边界

- `src/api/chat.js` 里保留 `generateTitle('/chat/title')` 封装，但 `backend/src/routes/register.js` 当前没有挂载 `/api/chat/title`；真实标题生成主要在 `useStreaming.js` 的前端 fallback 中完成。
- 前端流式解析兼容 `{ sources }`，独立 RAG 接口 `/api/rag/chat/stream` 会直接推 `sources`。
- 评测走 `/api/eval/run` SSE 接口，实际调用 RAG 管道后再用 `judge.service.js`（独立 API Key + step-3.5-flash）做 LLM-as-judge，失败自动降级关键词匹配。

---

## 1. 技术栈

### 前端

| 模块 | 技术/文件 |
| --- | --- |
| 应用框架 | Vue 3、Composition API、Vite |
| 状态管理 | Pinia：`src/stores/*.js` |
| 路由 | Vue Router：`src/router/index.js` |
| 样式/UI | Tailwind 风格 class、Lucide 图标、Element Plus icons 依赖 |
| 网络 | 原生 `fetch`，统一封装在 `src/api/client.js` |
| 流式通信 | Fetch + ReadableStream + TextDecoder，自行解析 SSE |
| Markdown | `markdown-it`、`highlight.js`、`DOMPurify` |
| 性能 | `vue-virtual-scroller` 依赖、`IntersectionObserver` 懒加载、Markdown Worker |
| 测试 | Vitest、Vue Test Utils、jsdom |

### 后端

| 模块 | 技术/文件 |
| --- | --- |
| 服务 | Node.js、Express：`backend/src/app.js` |
| 鉴权 | JWT + httpOnly cookie：`auth.middleware.js` |
| AI | OpenAI-compatible 请求，默认配置 StepFun：`ai.service.js`、`config/index.js` |
| RAG | BGE-small-zh ONNX + Milvus Hybrid Search + BGE-reranker-base：`rag.service.js`、`embedding.service.js`、`vector-store.service.js`、`reranker.service.js` |
| 评测 | LLM-as-judge（独立 Key + step-3.5-flash）：`judge.service.js`、`eval.routes.js` |
| 存储 | `REDIS_URL` 存在用 Redis；否则 MemoryStore + `backend/data/store.json` |
| 文件 | `multer`、`pdf-parse`、`mammoth` |
| 安全 | CORS 白名单、Helmet、rate limit、统一错误处理 |
| 部署 | Dockerfile、Docker Compose、Nginx、GitHub Actions |

---

## 2. 当前项目结构

```text
D:\Vue3_武理小精灵
├─ src/                         # 前端 Vue 代码
│  ├─ main.js                    # 创建 Vue app，注册 Pinia、Router、虚拟滚动、全局错误处理
│  ├─ App.vue                    # 全局壳层、侧边栏、页面标题、个人中心/Prompt/Skill/MCP 面板入口
│  ├─ router/index.js            # 路由表和登录守卫
│  ├─ views/                     # 页面级组件
│  │  ├─ Login.vue               # 教务账号登录
│  │  ├─ AIChat.vue              # 主聊天页
│  │  ├─ KnowledgeBase.vue       # RAG 文档管理页
│  │  ├─ EvalScoring.vue         # 评测与系统指标页
│  │  └─ Settings.vue            # 设置展示页
│  ├─ components/chat/           # 聊天输入、消息列表、Markdown、Agent 展示、Prompt/Skill/MCP
│  ├─ components/common/         # Toast、错误边界、登录弹窗、个人中心、头像选择
│  ├─ components/eval/           # RAGAS 看板、系统指标、评测内容查看
│  ├─ stores/                    # Pinia 状态模块
│  ├─ composables/               # 流式、消息操作、Markdown、评测指标等复用逻辑
│  ├─ api/                       # 前端 API 封装
│  ├─ utils/                     # 消息规范化、缓存、Markdown 配置、错误处理
│  ├─ workers/markdown.worker.js # Markdown Worker
│  └─ __tests__/                 # 前端单测
├─ backend/
│  ├─ src/app.js                 # Express 启动入口
│  ├─ src/routes/                # API 路由
│  ├─ src/controllers/           # chat/rag 控制器
│  ├─ src/services/              # Agent、RAG、教务、记忆、存储等服务
│  ├─ src/middleware/            # CORS、Helmet、鉴权、限流
│  └─ __tests__/                 # 后端单测
├─ scripts/rag-eval/             # RAG 评测脚本与数据集
├─ deploy/                       # Nginx、生产环境模板和部署脚本
├─ Dockerfile
├─ docker-compose.yml
├─ vite.config.js
├─ vitest.config.js
└─ package.json
```

---

## 3. 启动与页面渲染流程

### 3.1 前端启动链路

对应文件：`src/main.js`

```text
createApp(App)
  → createPinia()
  → app.use(pinia)
  → app.use(router)
  → app.use(VueVirtualScroller)
  → setupGlobalErrorHandler(app, toastStore)
  → app.mount('#root')
```

可讲点：

- `Pinia` 必须先注册，后面路由守卫和全局错误处理才能使用 store。
- 全局错误处理统一进入 `ToastManager`，避免组件各自写重复错误提示。
- `App.vue` 是布局壳层，页面内容通过 `<router-view>` 渲染，功能面板通过 `defineAsyncComponent` 懒加载。

### 3.2 路由结构

对应文件：`src/router/index.js`

| 路径 | 页面 | 登录要求 |
| --- | --- | --- |
| `/` | 重定向 `/chat` | 是 |
| `/login` | `Login.vue` | 否 |
| `/chat` | `AIChat.vue` | 是 |
| `/knowledge` | `KnowledgeBase.vue` | 是 |
| `/eval` | `EvalScoring.vue` | 是 |
| `/feedback` | `RagFeedback.vue` | 是（管理员） |
| `/settings` | `Settings.vue` | 是 |
| `/:pathMatch(.*)*` | 重定向 `/chat` | 是 |

守卫逻辑：

```text
目标路由 requiresAuth 且 authStore.isAuthenticated 为 false → /login
访问 /login 但已登录 → /chat
其他情况 → next()
```

### 3.3 登录态设计

对应文件：`src/views/Login.vue`、`src/stores/auth.store.js`、`backend/src/routes/school.routes.js`、`backend/src/middleware/auth.middleware.js`

真实流程：

```text
Login.vue 提交 studentId/password
  → POST /api/school/login
  → 后端用 Puppeteer/CAS 逻辑校验教务账号
  → 成功后生成 JWT，写入 auth_token httpOnly cookie
  → 后端加密保存教务密码到 store：school:user:<userId>
  → 前端 authStore.login(data.data.user)，只存 localStorage.user
```

面试说法：

> 我没有把 JWT 暴露给前端 JS，而是让后端写 httpOnly cookie；前端只保存 `user` 用于页面展示和路由判断。所有 API 请求通过 `credentials: 'include'` 自动带上 cookie，`401` 时统一清理本地用户并跳回登录页。

---

## 4. Pinia 状态管理设计

### 4.1 Store 拆分

| Store | 职责 |
| --- | --- |
| `auth.store.js` | 用户信息、登录态、退出登录、本地用户更新 |
| `chat.store.js` | 聚合会话与消息能力，给页面一个统一入口 |
| `conversation.store.js` | 会话列表、当前会话、后端会话加载、本地缓存、重命名/删除 |
| `message.store.js` | 流式状态、发送、重试、中断、清空/删除消息 |
| `toast.store.js` | 全局提示消息 |
| `theme.store.js` | 暗色模式和 `localStorage.darkMode` |
| `language.store.js` | 中英文文案 |
| `prompt.store.js` | 常用提示词管理 |
| `skill.store.js` | GitHub Skill 导入、启用、构建 XML 风格 `skillPrompt` |
| `mcp.store.js` | MCP Server 配置管理、导入/导出 |
| `eval.store.js` | 后端评测运行和指标计算状态 |

### 4.2 为什么拆 `conversation.store` 和 `message.store`

`conversation.store` 关注“数据结构”：

- 当前会话 ID：`chat_current_conversation_id`
- 会话缓存：`chat_cache`
- 后端会话接口：`/api/conversations`
- 本地 fallback：未登录或接口失败时创建 `local_` 会话
- 防抖保存：流式更新时通过 `scheduleSaveCache()` 减少同步写入

`message.store` 关注“过程状态”：

- `isLoading`、`currentStreamingId`、`activeStreamingConversationId`
- `sendMessage()`、`retryMessage()`、`abortCurrentRequest()`
- 删除/清空消息操作

`chat.store` 是页面聚合层：

- 页面只调用 `chatStore.sendMessage()`、`chatStore.switchConversation()` 等方法。
- 切换会话时，如果另一个会话仍在流式输出，会先调用 `abortCurrentRequest()`，防止 UI 状态错乱。

面试说法：

> 我把“会话数据”和“流式副作用”拆开，是为了避免一个 store 同时承担数据建模、网络请求和 UI 状态。页面通过 `chat.store` 使用统一接口，内部再组合 conversation/message 两个 store。

---
## 5. AI 聊天完整链路

### 5.1 前端入口

相关文件：

- `src/views/AIChat.vue`
- `src/components/chat/ChatBox.vue`
- `src/components/chat/MessageList.vue`
- `src/components/chat/MessageBubble.vue`
- `src/stores/chat.store.js`
- `src/composables/useStreaming.js`
- `src/api/chat.js`

链路：

```text
用户在 ChatBox 输入/选择附件
  → AIChat.vue 触发发送
  → chatStore.sendMessage(text, retryMsgId, fileData)
  → messageStore.sendMessage
  → useStreaming.sendMessage
  → 插入用户消息 + 空 AI 消息
  → sendMessageStream('/api/stream')
  → 前端读取 SSE chunk
  → 更新 AI 消息 content/thinkingSteps/toolCalls/trace
  → MessageList/MessageBubble 响应式刷新
```

### 5.2 `useStreaming.sendMessage()` 做了什么

对应文件：`src/composables/useStreaming.js`

1. 校验输入：空文本且无附件直接返回；正在 loading 时不重复发送。
2. 定位当前会话；没有会话时创建本地会话。
3. 插入用户消息：`role: 'user'`，附件存在时写入 `files`。
4. 插入空 AI 消息：`role: 'model'`，预留 `sources/toolCalls/thinkingSteps/trace`。
5. 构造历史上下文：过滤 welcome/error，最多取最近 20 条，并避免连续同 role。
6. 从 `skill.store` 构建 `skillPrompt`。
7. 如果上传文档解析出 `textContent`，把文件内容拼进用户消息上下文。
8. 调用 `sendMessageStream()`，并绑定 `onChunk/onThinking/onToolCall/onToolResult/onTrace/onDone/onError` 回调。
9. chunk 回调中用 `requestAnimationFrame` 合并文本更新，减少频繁响应式刷新。
10. 完成后根据首条用户消息生成短标题；当前主要是前端 fallback，而不是后端 `/chat/title`。

### 5.3 SSE 解析逻辑

对应文件：`src/api/chat.js`

请求体：

```text
POST /api/stream
body: { message, history, conversationId, files, skillPrompt }
```

前端读取方式：

```text
fetch('/api/stream')
  → response.body.getReader()
  → TextDecoder('utf-8') 增量解码
  → buffer 按 \n 拆行
  → 只处理 data: 开头的行
  → data: [DONE] 表示结束
  → JSON.parse(data) 后分发回调
```

前端兼容的事件：

| 事件字段 | 回调 | 当前默认聊天流是否会发 |
| --- | --- | --- |
| `{ content }` | `onChunk` | 会 |
| `{ thinking }` | `onThinking` | 会 |
| `{ tool_call }` | `onToolCall` | 会 |
| `{ tool_result }` | `onToolResult` | 会 |
| `{ trace }` | `onTrace` | 会 |
| `{ sources }` | `onSources` | 前端兼容，但默认 `/api/stream` 当前不直接发；RAG stream 会发 |
| `{ error }` | `onError` | 异常时会 |
| `[DONE]` | `onDone` | 会 |

稳定性设计：

- `connectionManager` 每 30 秒请求 `/api/health` 做心跳。
- 流式请求有 60 秒 stall 检测，长时间无数据会取消 reader。
- 网络失败会指数退避重试，最多 3 次。
- 请求被 `AbortController` 中断时调用 `onAbort`，不会误标失败。

### 5.4 后端流式输出

相关文件：`backend/src/routes/register.js`、`backend/src/controllers/chat.controller.js`、`backend/src/services/agent.service.js`

后端链路：

```text
POST /api/stream
  → register.js 挂载 streamHandler
  → streamHandler 设置 text/event-stream / no-cache / keep-alive / X-Accel-Buffering:no
  → getAgentService().chatStream(message, history, { userId, skillPrompt, files, signal })
  → for await 读取 Agent chunk
  → switch chunk.type 写成 SSE data 行
  → content.done 时写 data: [DONE]
```

注意当前真实实现：

- `streamHandler` 当前没有把 `conversationId` 传给 `AgentService.chatStream`，这是后端 Working Memory 按会话隔离的待优化点。
- 默认聊天流不直接转发 RAG `sources`，知识库路径会通过 `tool_result` 和最终 `content` 展示结果。

面试高分回答：

> 我把 AI 回复抽象成事件流，而不是只返回纯文本。前端先插入一条空 AI 消息，后端每推一个 `content` chunk 就追加文本；如果有 Agent 思考、工具调用或 trace，也挂到同一条消息上。这样既有实时生成体验，也能展示工具链路。

---

## 6. 文件上传聊天

相关文件：`src/api/chat.js`、`src/composables/useStreaming.js`、`backend/src/routes/register.js`、`backend/src/services/file-upload.service.js`

流程：

```text
用户选择文件
  → uploadChatFile(file)
  → POST /api/chat/upload (FormData)
  → requireAuth 校验登录
  → multer 保存到 backend/uploads
  → 图片只返回 URL/元信息
  → PDF/DOC/DOCX/TXT/MD 解析 textContent
  → 前端把 textContent 包装进 messageToSend
```

支持类型：

- 聊天附件：文档 + 图片，图片扩展名包括 `jpg/jpeg/png/gif/webp`。
- 知识库上传：`pdf/docx/doc/txt/md`。
- 非图片上传资源对外访问时强制下载，并设置 `X-Content-Type-Options: nosniff`。

面试说法：

> 文档类附件不是只上传展示，而是在后端解析为文本后返回给前端，前端再把文件内容作为上下文拼进本次问题；图片会传入文件列表，后端检测到图片时强制走 chat 路径，具体多模态效果取决于模型服务能力。

---

## 7. RAG 检索链路

### 7.1 相关文件

| 文件 | 作用 |
| --- | --- |
| `backend/src/services/rag.service.js` | 检索管道总调度：混合检索→归并→rerank→截断→排序→组装 |
| `backend/src/services/embedding.service.js` | BGE-small-zh ONNX 稠密 512d + n-gram 稀疏（整数 key 哈希） |
| `backend/src/services/vector-store.service.js` | Milvus hybrid search 操作 |
| `backend/src/services/reranker.service.js` | BGE-reranker-base cross-encoder |
| `backend/src/services/indexing.service.js` | 文档切片：中文章节合并→段落→句子 |
| `backend/src/services/document.service.js` | 文档 CRUD |
| `backend/src/services/judge.service.js` | LLM-as-judge（独立 Key + step-3.5-flash） |

### 7.2 检索流程

```text
用户提问
  ↓
BGE-small-zh ONNX → 稠密 512d + n-gram 稀疏向量
  ↓
Milvus Hybrid Search (topK=50)
  稠密 COSINE ×0.6 + 稀疏 IP ×0.4
  ↓
50 句子 → 按 parentId 归并 → ~15 个父段落
  ↓
BGE-reranker-base cross-encoder 逐对打分 → sigmoid
  ↓
自适应截断（断崖>0.05 + 低分<0.3 + 硬上限=rerankTopK）
  ↓
二级排序 (docId, parentIdx)
  ↓
上下文组装 max 6000 字
  ↓
StepFun step-3.7-flash → 回复 + sources
```

### 7.3 评测体系

| 环节 | 工具 | 指标 |
| --- | --- | --- |
| 检索质量 | `eval-retrieval.js` | Recall@K, Precision@K, MRR, nDCG@5 |
| 生成质量 | `judge.service.js` + step-3.5-flash | Faithfulness, Relevancy, Precision, Recall |
| 人工评分 | `EvalScoring.vue` | 1-5 分 |
| 线上反馈 | `RagFeedback.vue` | like/dislike 统计 |
| 回归评估 | 基线比对 | 指标变化 > 2% 告警 |

面试说法：

> 评测方面，我们建了一套离线测试集（32 题覆盖 6 个文档），跑检索指标作为基线。LLM-as-judge 用独立 API Key 和 step-3.5-flash 模型，跟生产流量完全隔离，4 个指标合并到 1 次请求，失败自动降级到关键词匹配，不卡死。

> 这个项目的 Agent 不是一个黑盒。确定性强的教务查询走 `simple` 单工具路径，复杂选课问题走 ReAct 多步推理，知识类问题走 RAG 工具，普通问题再让模型处理。这样能降低成本、提高可解释性，也方便前端展示工具调用过程。

---

## 8. RAG 知识库流程

### 8.1 相关文件

| 文件 | 作用 |
| --- | --- |
| `src/views/KnowledgeBase.vue` | 文档列表、搜索、分类、上传、预览、删除 |
| `src/api/rag.js` | RAG API 封装 |
| `backend/src/routes/rag.routes.js` | `/api/rag` 路由 |
| `backend/src/controllers/rag.controller.js` | RAG 控制器 |
| `backend/src/services/document.service.js` | 文档元数据、切片、索引状态 |
| `backend/src/services/indexing.service.js` | 生成 embedding 并写入向量库 |
| `backend/src/services/embedding.service.js` | BGE-small-zh ONNX 稠密 512d + n-gram 稀疏（整数 key） |
| `backend/src/services/vector-store.service.js` | Milvus hybrid search |
| `backend/src/services/reranker.service.js` | BGE-reranker-base cross-encoder |
| `backend/src/services/rag.service.js` | 混合检索、融合、rerank、自适应截断、父文档召回、上下文组装、生成回答 |

### 8.2 文档上传流程

```text
KnowledgeBase.vue
  → uploadFile(file, category, title)
  → POST /api/rag/documents/upload
  → multer 保存文件
  → parseFile 解析 PDF/DOCX/TXT/MD
  → DocumentService.addDocument
  → TextSplitter 按段切片，chunkSize=500，overlap=50
  → store 保存 doc 元数据和全文
  → IndexingService 生成 embedding
  → VectorStoreService 写入 ChromaDB；不可用则内存模式
  → 返回 chunkCount/vectorStatus
```

### 8.3 查询流程

```text
Agent knowledge route 或 /api/rag/chat
  → RagService.retrieveCandidates
  → EmbeddingService.embed(query)
  → VectorStore.search 向量召回
  → KeywordSearchService.search BM25 召回
  → fuseRetrievalResults 做 RRF + vector/keyword 权重融合
  → selectTopChunks 做轻量 rerank
  → assembleParentContext 回到父文档全文
  → buildParentChildPrompt 要求模型基于参考资料回答并引用文档编号
  → 返回 reply/sources/context/model
```

### 8.4 当前 RAG 设计亮点

- **混合检索**：向量召回适合语义相似，BM25 适合关键词精确命中，两路结果融合。
- **RRF 融合**：除了向量/关键词分数，还加入排名融合分数，降低单一路径偏差。
- **轻量 rerank**：不是额外调用 rerank 模型，而是在融合分数基础上叠加标题/内容查询词覆盖率。
- **父子召回**：子块用于命中，父文档全文用于生成，回答上下文更完整。
- **可靠来源策略**：低于 `minSourceScore` 时返回“资料不足”类回答，不强行编造。
- **降级能力**：Embedding API 失败时用本地 n-gram embedding；ChromaDB 不可用时用内存向量库。

面试说法：

> 当前 RAG 主链路已经从旧的 ChatDoc 方案切到了本地混合检索。上传文档后会解析、切片、生成 embedding 并写入向量库；查询时同时走向量和 BM25，再做 RRF 融合、轻量 rerank 和父文档召回，最后让模型基于参考资料回答。

---
## 9. 教务系统功能

相关文件：`src/api/school.js`、`src/views/Login.vue`、`backend/src/routes/school.routes.js`、`backend/src/services/school-session.service.js`、`backend/src/services/school-api.service.js`

| 接口 | 作用 |
| --- | --- |
| `POST /api/school/login` | 教务账号登录，成功后写 httpOnly cookie |
| `POST /api/school/bind` | 登录后重新绑定教务账号 |
| `GET /api/school/status` | 查询绑定状态 |
| `DELETE /api/school/bind` | 解除绑定 |
| `GET /api/school/grades` | 成绩查询 |
| `GET /api/school/schedule` | 课表查询 |
| `GET /api/school/exams` | 考试安排 |
| `GET /api/school/semesters` | 学期列表 |

与 Agent 的关系：

- 用户可以在聊天里说“查一下我的成绩”“今天有什么课”“考试安排是什么”。
- `IntentRouter` 会把这类问题路由到 `simple`。
- `agent-tools.js` 调用 `school-api.service.js`，后端拿已加密保存的教务凭据查询数据。
- 前端通过 `AgentToolCall.vue` 展示工具开始和结果。

---

## 10. 会话管理与持久化

### 10.1 数据结构

消息对象典型字段：

```js
{
  id: '...',
  role: 'user' | 'model',
  content: '...',
  text: '...',
  timestamp: Date,
  files: [],
  sources: [],
  toolCalls: [],
  thinkingSteps: [],
  trace: null,
  isError: false,
  canRetry: false
}
```

会话对象典型字段：

```js
{
  id: 'local_xxx' | '后端会话 id',
  title: '新会话',
  messages: [],
  createdAt: Date,
  updatedAt: Date
}
```

### 10.2 本地与后端双存储

- 未登录或后端不可用：`conversation.store` 创建 `local_` 会话，保存到 `localStorage.chat_cache`。
- 已登录：先从本地缓存恢复，再请求 `/api/conversations` 合并服务端会话。
- 页面关闭前：`beforeunload` 刷入缓存。
- 流式更新时：300ms 防抖增量保存，避免每个 chunk 都全量序列化。
- 兼容迁移：读取旧 key `chat_messages_backup`、`chat_msgs_direct`、`chat_local_conversations_cache` 后清理。

### 10.3 流式切换保护

对应文件：`src/stores/chat.store.js`、`src/composables/useStreaming.js`

保护点：

1. 切换会话时，`chat.store` 检查 `activeStreamingConversationId`，如果和目标会话不同就中断旧请求。
2. `onChunk` 回调再次检查 `convStore.currentConversationId !== conversationId`，如果用户已切走就 `abortCurrentRequest()`。
3. `updateMessage()` 每次按 `conversationId` 重新找会话下标，不缓存旧下标，避免列表重排后写错会话。

面试说法：

> 长对话应用很容易出现“旧请求写到新会话”的问题。我在切换入口和 chunk 回调两处都做了保护，并且更新消息时按会话 ID 重新定位，而不是缓存数组下标。

---

## 11. Markdown、代码高亮与代码运行

### 11.1 Markdown 渲染

相关文件：`MarkdownRenderer.vue`、`useMarkdownRenderer.js`、`useMarkdownWorker.js`、`markdownConfig.js`、`markdown.worker.js`

流程：

```text
AI content
  → completeMarkdown 补全未闭合语法
  → markdown-it 转 HTML
  → highlight.js 高亮代码块
  → DOMPurify 按白名单清洗
  → v-html 渲染
```

性能与安全：

- 内容长度超过 2000 字符时优先走 Web Worker 渲染。
- Worker 不可用或 5 秒无响应时回退主线程。
- 链接规则统一加安全属性。
- `DOMPurify` 白名单集中在 `markdownConfig.js`，避免模型输出任意 HTML。

### 11.2 代码块和运行器

相关文件：`CodeBlock.vue`、`CodeRunner.vue`

- `CodeBlock` 支持复制和运行按钮。
- `CodeRunner` 当前只支持 JavaScript / js。
- 执行环境是隐藏 iframe，`sandbox="allow-scripts"`，并设置 CSP：`default-src 'none'; script-src 'unsafe-inline'; style-src 'unsafe-inline'`。
- 通过 `postMessage` 把 console、错误和耗时传回父页面。

面试边界：

> 代码运行是前端 iframe 沙箱里的轻量演示能力，不是后端任意代码执行平台。真实生产如果要支持多语言运行，需要独立的安全沙箱和资源限制。

---

## 12. Prompt、Skill、MCP 配置

| 模块 | 文件 | 当前能力 |
| --- | --- | --- |
| Prompt | `PromptPanel.vue`、`prompt.store.js` | 本地管理常用提示词、分类、复制/选择 |
| Skill | `SkillPanel.vue`、`skill.store.js` | 从 GitHub Markdown 链接导入 Skill，最多取启用的前 4 个构建 XML 风格系统提示 |
| MCP | `McpPanel.vue`、`mcp.store.js` | 管理 MCP server 配置，支持导入/导出 Claude Desktop 风格配置 |

面试说法：

> Prompt、Skill、MCP 这几个面板目前主要是前端配置管理能力。Skill 会真正参与发送链路：`useStreaming` 发送消息前调用 `skill.store.buildSystemPrompt()`，把启用的技能规范作为 `skillPrompt` 传给后端。

---

## 13. 评测与可观测性

相关文件：`src/views/EvalScoring.vue`、`src/composables/useEvalData.js`、`src/composables/useSystemMetrics.js`、`src/stores/eval.store.js`、`backend/src/routes/eval.routes.js`、`backend/src/services/judge.service.js`、`backend/src/services/metrics.service.js`、`scripts/rag-eval/`

能力：

- **LLM-as-judge**：`judge.service.js` 使用独立 API Key + step-3.5-flash，4 指标合并到 1 次请求，失败自动降级关键词匹配
- **检索指标**：`eval-retrieval.js` 输出 Recall@K、Precision、MRR、nDCG@K
- 人工评分：`EvalScoring.vue` 支持 1-5 分打分、键盘快捷键、导入导出
- 线上反馈：`RagFeedback.vue` 收集用户 like/dislike，支持分页筛选
- 系统指标：`/api/eval/metrics` 每 5 秒轮询，记录 embedding/vectorSearch/rerank/ai 等阶段耗时
- 回归评估：基线比对，指标变化 > 2% 告警

面试说法：

> 我不只做功能，也做了评测和可观测性。RAG 查询会记录检索阶段和生成阶段耗时、命中文档数、是否用了父子召回，后续优化可以基于指标，而不是只靠主观感觉。

---

## 14. 后端 API 总览

### 14.1 基础接口

| 接口 | 作用 |
| --- | --- |
| `GET /api/health` | 健康检查 |
| `GET /api` | API 列表 |
| `GET /api/usage` | 兼容接口，返回空 usage summary |
| `POST /api` | 非流式聊天 |
| `POST /api/chat` | 非流式聊天兼容接口 |
| `POST /api/stream` | 默认 Agent SSE 流式聊天 |
| `POST /api/auth/logout` | 清除 httpOnly cookie |
| `POST /api/chat/upload` | 登录后聊天附件上传 |

### 14.2 子路由

| 前缀 | 文件 | 作用 |
| --- | --- | --- |
| `/api/conversations` | `conversations.routes.js` | 会话列表、创建、详情、更新、删除、清空消息 |
| `/api/rag` | `rag.routes.js` | RAG 聊天、文档上传/新增/批量新增/列表/详情/删除/统计 |
| `/api/school` | `school.routes.js` | 教务登录、绑定、成绩、课表、考试、学期 |
| `/api/eval` | `eval.routes.js` | 系统指标、评测运行、指标计算、健康检查 |
| `/api/tools` | `tool.routes.js` | 工具列表、详情、开关、删除、执行 |
| `/api/memory` | `memory.routes.js` | 记忆列表、统计、长期记忆、用户画像、清空 |
| `/api/agent` | `agent.routes.js` | 最近 Agent trace 查询 |

### 14.3 不要误讲的接口

- `/api/chat/title`：控制器里有 `generateTitle`，但当前路由没有挂载；前端标题主要 fallback 生成。
- `/api/rag/chat/stream`：存在并能推 `sources/retrieval/content`，但主聊天页默认走 `/api/stream`。

---
## 15. 安全设计

| 场景 | 当前做法 |
| --- | --- |
| 登录态 | 后端写 `auth_token` httpOnly cookie，前端只存 `localStorage.user` |
| Cookie 请求 | API 封装默认 `credentials: 'include'` |
| 401 | `api/client.js` 调用 logout、清 user、跳 `/login` |
| CORS | 开发允许 `localhost:5173`；生产必须配置 `CORS_ORIGIN`，否则 fail-fast |
| 限流 | 聊天接口、登录/绑定接口使用 `express-rate-limit` |
| Helmet | 后端启用 Helmet，并针对开发环境做兼容配置 |
| 上传 | 扩展名 + MIME 校验，聊天上传需要登录 |
| 上传资源 | 图片可内联；非图片强制下载并 `nosniff` |
| Markdown | DOMPurify 白名单清洗模型输出 |
| RAG | 无可靠来源时拒绝编造 |
| 密码 | 教务密码通过 `school-session.service` 加密后保存到 store |

面试高分回答：

> 大模型应用的安全重点不只是登录，还包括“模型输出不可信”和“用户上传不可信”。所以项目在 Markdown 渲染、上传文件访问、httpOnly cookie、CORS 白名单、限流和 RAG 无来源策略上都做了边界处理。

---

## 16. 性能优化点

| 优化 | 对应实现 |
| --- | --- |
| 流式输出 | 首 token 到达即可展示，不等完整响应 |
| RAF 合并 chunk | `useStreaming` 用 `requestAnimationFrame` 合并文本更新 |
| 防抖缓存 | 会话缓存 300ms 防抖增量保存，避免每个 chunk 同步写 localStorage |
| Markdown Worker | 长内容超过 2000 字符走 Worker 渲染 |
| 懒加载旧消息 | `MessageList.vue` 对较早消息包 `LazyMessage`，用 IntersectionObserver 延迟渲染 |
| 手动分包 | `vite.config.js` 把 vue、markdown、highlight、icons、virtual-scroller 拆 chunk |
| API 心跳和重试 | `connectionManager` 心跳 + 指数退避重试 |

---

## 17. 测试、构建与部署

### 常用命令

| 命令 | 作用 |
| --- | --- |
| `npm install` | 安装前端依赖，并通过 `postinstall` 安装后端依赖 |
| `npm run dev` | 启动 Vite，端口 5173，代理 `/api` 到 3000 |
| `npm start` | 进入 `backend` 启动 Express |
| `npm run build` | Vite 构建 `dist` |
| `npm run lint:check` | ESLint 检查前端 `src/` |
| `npm test` | Vitest 跑前后端测试 |

### 测试覆盖

- 前端：`authStore`、`chatStore`、`chatHelpers`、`crypto`、`errorHandler`、`useCodeHighlighter`、`useLazyload`。
- 后端：Agent trace、会话路由、意图路由、memory store/service、中间件、RAG service、ReAct stream、school session、tool registry、working memory。

### 构建部署

- `vite.config.js`：开发代理 `/api → http://localhost:3000`，proxy timeout 120s。
- `Dockerfile`：多阶段构建，先构建前端 `dist`，再构建后端镜像并复制静态资源。
- `docker-compose.yml`：编排 backend、Redis、Nginx 等服务。
- `.github/workflows/deploy.yml`：Lint/Test → Build/Push Docker Image → SSH 部署到 ECS。
- 具体公网 IP 不建议在面试里背死，以实际部署环境为准。

---

## 18. 面试常问问题与高分回答

### Q1：这个项目最核心的难点是什么？

> 核心难点是把 AI 流式输出、前端多会话状态、Agent 工具事件和 RAG 引用组合到一个稳定的聊天体验里。尤其是流式过程中用户可能切换会话、删除消息或网络中断，所以我做了会话 ID 定位、AbortController 中断、RAF 合并更新和本地缓存防抖。

### Q2：为什么不用普通 HTTP 等完整回答返回？

> AI 回复可能很慢，完整返回会让用户长时间看不到反馈。流式 SSE 可以边生成边展示，并且能把 `thinking/tool_call/tool_result/trace` 这类结构化事件一起推给前端，体验和可解释性都更好。

### Q3：Pinia 为什么这样拆？

> 会话数据和流式副作用变化频率、职责都不同。`conversation.store` 管会话结构和持久化，`message.store` 管发送、重试、中断，`chat.store` 做聚合。这样页面简单，内部也更容易测试和维护。

### Q4：如何避免流式输出写错会话？

> 首先切换会话时如果旧会话仍在流式输出，会主动 abort；其次 chunk 回调里再次判断当前会话 ID 是否还是发起时的会话；最后更新消息时按 conversationId 重新查找会话，而不是缓存数组下标。

### Q5：RAG 是怎么实现的？

> 当前 RAG 是本地混合检索。文档上传后解析成文本，按段切片，生成 embedding 写入向量库；查询时同时做向量召回和 BM25 召回，用 RRF 和权重融合，再做轻量 rerank，最后用命中的子块找到父文档全文构造增强 prompt，让模型基于参考资料回答。

### Q6：Agent 和普通聊天有什么区别？

> 普通聊天是直接让模型回答；Agent 会先做意图路由。成绩、课表、考试这种确定性任务走工具，选课可行性这类复杂任务走 ReAct，多文档问题走 RAG，普通问题才走通用 Agent/LLM。这样准确率、成本和可解释性都更可控。

### Q7：当前有哪些可优化点？

1. `/api/stream` 需要把 `conversationId` 透传给 `AgentService`，让 Working Memory 真正按会话隔离。
2. 默认聊天流可以补充 `sources/retrieval` 事件转发，让知识库引用展示更直接。
3. `/api/chat/title` 可以正式挂载或删除前端旧封装，避免接口预期不一致。
4. RAG 可以引入更强的 rerank 模型和 query rewrite。
5. MCP 前端配置可以进一步打通后端 Tool Registry。
6. 生产环境可以补更完整的日志、监控、HTTPS 和 CSRF 防护。

---

## 19. 两分钟项目讲解稿

> 我做的项目叫武理小精灵，是一个面向武汉理工大学校园场景的 AI 助手。前端用 Vue 3、Pinia 和 Vite 实现，核心页面包括聊天、知识库、评测、设置和登录。聊天页支持流式输出、文件上传、Markdown 渲染、代码高亮、多会话管理，以及 Agent 工具调用过程展示。
>
> 前端这边我重点做了状态拆分和流式体验。`chat.store` 是页面统一入口，内部组合 `conversation.store` 和 `message.store`。发送消息时先插入用户消息和空 AI 消息，然后通过 Fetch 读取 SSE，每个 chunk 到达后更新同一条 AI 消息。为了避免长对话卡顿，我还做了 RAF 合并更新、会话缓存防抖、Markdown Worker 和旧消息懒加载。
>
> 后端不是简单转发大模型，而是有一套 Agent 意图路由。用户消息进入 `AgentService` 后，会先识别是成绩课表查询、知识库问答、成绩分析、选课推理还是普通对话。确定性查询走工具，复杂任务走 ReAct，知识问题走 RAG，最后把工具调用、思考过程和 trace 通过流式事件返回前端。
>
> RAG 知识库当前是本地混合检索方案。文档上传后解析、切片、生成 embedding，写入向量库；查询时同时做向量召回和 BM25，再做融合、轻量重排和父文档召回，让模型基于参考资料回答。工程上还做了 httpOnly cookie 鉴权、上传文件安全处理、DOMPurify 清洗、Vitest 测试、Docker 和 Nginx 部署配置。

---

## 20. 复习顺序

1. 先背第 0 节：一句话、亮点和真实边界。
2. 再背第 5 节：聊天流式链路，这是最容易被追问的。
3. 然后背第 7、8 节：Agent 和 RAG，这是项目差异化。
4. 接着背第 4、10 节：Pinia 拆分、多会话和缓存。
5. 最后背第 15、16、17 节：安全、性能、测试部署。

---

## 21. 文件定位清单

| 想讲的点 | 直接看哪个文件 |
| --- | --- |
| 应用启动 | `src/main.js` |
| 全局布局 | `src/App.vue` |
| 路由守卫 | `src/router/index.js` |
| 登录页 | `src/views/Login.vue` |
| 登录态 | `src/stores/auth.store.js`、`src/api/client.js` |
| 聊天页 | `src/views/AIChat.vue` |
| 聊天输入 | `src/components/chat/ChatBox.vue` |
| 消息列表 | `src/components/chat/MessageList.vue`、`src/components/chat/LazyMessage.vue` |
| 消息气泡 | `src/components/chat/MessageBubble.vue` |
| Markdown | `src/components/chat/MarkdownRenderer.vue`、`src/composables/useMarkdownRenderer.js` |
| 代码运行 | `src/components/chat/CodeBlock.vue`、`src/components/chat/CodeRunner.vue` |
| 会话状态 | `src/stores/conversation.store.js`、`src/utils/conversationCache.js` |
| 流式发送 | `src/composables/useStreaming.js`、`src/api/chat.js` |
| Store 聚合 | `src/stores/chat.store.js`、`src/stores/message.store.js` |
| Prompt/Skill/MCP | `src/stores/prompt.store.js`、`src/stores/skill.store.js`、`src/stores/mcp.store.js` |
| 知识库页面 | `src/views/KnowledgeBase.vue`、`src/api/rag.js` |
| 评测页面 | `src/views/EvalScoring.vue`、`src/composables/useSystemMetrics.js` |
| 后端路由注册 | `backend/src/routes/register.js` |
| 后端聊天控制器 | `backend/src/controllers/chat.controller.js` |
| RAG 核心 | `backend/src/services/rag.service.js` |
| Embedding | `backend/src/services/embedding.service.js` |
| 向量检索 | `backend/src/services/vector-store.service.js` |
| Reranker | `backend/src/services/reranker.service.js` |
| LLM-as-judge | `backend/src/services/judge.service.js` |
| 文档管理 | `backend/src/services/document.service.js`、`backend/src/services/indexing.service.js` |
| 教务接口 | `backend/src/routes/school.routes.js`、`backend/src/services/school-api.service.js` |
| 鉴权中间件 | `backend/src/middleware/auth.middleware.js` |
| 后端通用中间件 | `backend/src/middleware/index.js` |
| 存储降级 | `backend/src/services/memory-store.js`、`backend/src/services/redis-store.js` |
| 评测接口 | `backend/src/routes/eval.routes.js`、`backend/src/services/judge.service.js`、`backend/src/services/metrics.service.js` |
| 构建代理 | `vite.config.js` |
| 测试配置 | `vitest.config.js` |
| 部署 | `Dockerfile`、`docker-compose.yml`、`deploy/nginx.conf`、`.github/workflows/deploy.yml` |
